# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SSSKV' do
  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!
  pod 'Protobuf', '~> 3.5.0'
  # Pods for SSSKV

  target 'SSSKVTests' do
    inherit! :search_paths
    pod 'Protobuf', '~> 3.5.0'
    # Pods for testing
  end

end
